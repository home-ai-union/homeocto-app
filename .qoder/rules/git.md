---
trigger: always_on
---
When generating git commits:
- Always write commit messages in English.
- Never modify the Git `user.name` or `user.email` configurations.
- main branch is protected,always modify and push develop branch 
